# Скрипты и конфигурации для сервера обязаны храниться с переводами строк LF:
# с CR в конце строк bash на Linux их не запустит.
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
*.py text eol=lf

# Батник для Windows — наоборот.
*.cmd text eol=crlf
